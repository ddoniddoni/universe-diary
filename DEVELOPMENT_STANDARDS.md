# universe-diary 개발 목적과 완료 기준

## 목적

기록이 별이 되고, 기록이 쌓일수록 사용자의 우주가 자라는 경험을 안정적이고 신뢰할 수 있게 제공한다.

## 기능 완료의 정의

기능은 코드가 존재한다고 완료가 아니다. 다음을 모두 만족할 때만 완료로 보고한다.

- 사용자가 진입할 수 있고, 다음 행동과 취소/되돌아가기 경로가 있다.
- 로딩·성공·실패·빈 상태가 명확하다.
- 서버 권한 검사와 입력 검증이 있다.
- 모바일과 데스크톱에서 역할이 분명하게 보인다.
- `lint`와 `build`를 통과하고, 가능한 경우 실제 흐름을 확인한다.

## 구현 원칙

- 화면은 작은 단위 컴포넌트와 의미 있는 타입으로 작성한다. 압축된 한 줄 JSX는 금지한다.
- 독립적인 서버 데이터는 `Promise.all`로 병렬 조회한다.
- 클라이언트로 전달하는 데이터는 필요한 필드만 직렬화한다.
- 인증은 API와 페이지 모두 서버에서 확인한다.
- UX 완성 전에는 “완료”라고 말하지 않는다.

## 현재 우선순위

1. MVP의 남은 기능을 PRD 완료 기준까지 구현한다.
2. 실제 사용 흐름을 검증한다.
3. 그 뒤 UI/UX를 화면 단위로 재설계하고 검증한다.
