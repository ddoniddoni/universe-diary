"use client";
import { useRouter } from "next/navigation";
import { FormEvent, useState } from "react";
const emotions = [["HAPPY","행복"],["CALM","평온"],["SAD","슬픔"],["ANGRY","화남"],["EXCITED","설렘"],["TIRED","지침"]] as const;
export function DiaryForm() { const router = useRouter(); const [error,setError]=useState(""); const [pending,setPending]=useState(false); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setPending(true);setError("");const data=Object.fromEntries(new FormData(e.currentTarget));const res=await fetch("/api/diaries",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const json=await res.json();setPending(false);if(!res.ok)return setError(json.message);router.push("/universe");router.refresh();} return <form onSubmit={submit} className="w-full space-y-6 rounded-3xl border border-white/10 bg-[#0b1026]/80 p-8"><p className="text-sm tracking-[.2em] text-[#7BDFF2]">TODAY&apos;S STAR</p><h1 className="text-3xl font-semibold">오늘의 별을 남겨볼까요?</h1><label className="block">제목<input required name="title" maxLength={50} className="mt-2 w-full rounded-xl bg-white/5 p-3" /></label><label className="block">오늘의 이야기<textarea required name="content" maxLength={5000} rows={8} className="mt-2 w-full rounded-xl bg-white/5 p-3" /></label><fieldset><legend>오늘의 감정</legend><div className="mt-3 grid grid-cols-3 gap-2">{emotions.map(([value,label])=><label key={value} className="cursor-pointer"><input required className="peer sr-only" type="radio" name="emotion" value={value}/><span className="block rounded-xl border border-white/10 p-3 text-center transition peer-checked:border-[#ffd166] peer-checked:bg-[#ffd166]/15 peer-checked:text-[#ffd166]">{label}</span></label>)}</div></fieldset>{error&&<p className="text-[#ffafcc]">{error}</p>}<button disabled={pending} className="w-full rounded-xl bg-[#ffd166] p-3 font-medium text-[#050510]">{pending?"별을 띄우는 중…":"오늘의 별 만들기"}</button></form>; }
