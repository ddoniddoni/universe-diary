"use client";
import { useState } from "react";
const stars=[[-280,-120,"#FFD166"],[-80,20,"#7BDFF2"],[150,-80,"#FFAFCC"],[330,110,"#B8A1FF"]] as const;
export function UniverseMap(){const [p,setP]=useState({x:0,y:0});const [s,setS]=useState({x:0,y:0});return <div className="absolute inset-0 z-10 cursor-grab overflow-hidden active:cursor-grabbing" onPointerDown={e=>setS({x:e.clientX,y:e.clientY})} onPointerMove={e=>{if(!s.x)return;setP(v=>({x:v.x+e.clientX-s.x,y:v.y+e.clientY-s.y}));setS({x:e.clientX,y:e.clientY})}} onPointerUp={()=>setS({x:0,y:0})}><div className="absolute inset-0" style={{transform:`translate(${p.x}px,${p.y}px)`}}><svg className="absolute inset-0 h-full w-full"><path d="M220 340 C410 180 570 480 760 340 S950 260 1110 430" fill="none" stroke="#B8A1FF" strokeOpacity=".5" strokeWidth="18" /></svg>{stars.map(([x,y,c])=><span key={x} className="absolute size-5 rounded-full" style={{left:`calc(50% + ${x}px)`,top:`calc(50% + ${y}px)`,backgroundColor:c,boxShadow:`0 0 24px ${c}`}}/>)}</div></div>}
